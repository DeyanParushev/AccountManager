import React, { Component } from 'react';

export default class Login extends Component {
    constructor(params) {
        super(params);
    }

    render() {
        return (
            <h1>Login page</h1>
        )
    }
}
